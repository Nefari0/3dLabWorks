import './Project.css'

const DlUrl = (props) => {

    // const { url } = this.props

    return(
        <div>
            {/* <text className="dark-text">{props.DlUrl}</text> */}
        </div>
    )
}

export default DlUrl